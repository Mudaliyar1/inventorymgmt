@model ProductCreateViewModel
@{
    ViewData["Title"] = "Add Product — Mobile Shop Catalog";
}

<div class="ph">
    <div>
        <h1 class="ph-title">Add Mobile Product / Accessory</h1>
        <p class="ph-sub">Register a new smartphone model or accessory in the mobile shop catalog.</p>
    </div>
    <div class="ph-actions">
        <a href="@Url.Action("Index","Product")" class="btn-d btn-outline">
            <i class="bi bi-arrow-left"></i> Back to Products
        </a>
    </div>
</div>

<form asp-action="Create" method="post" enctype="multipart/form-data">
    @Html.AntiForgeryToken()
    <div class="row g-4">
        <!-- Left Column -->
        <div class="col-lg-8">
            <div class="card-d card-p-lg mb-4">
                <div class="s-title"><i class="bi bi-phone me-2 text-primary"></i>Mobile Device & Product Information</div>
                <div asp-validation-summary="ModelOnly" class="alert-d alert-danger mb-4"></div>

                <div class="row g-3">
                    <div class="col-md-6">
                        <div class="fg">
                            <label class="fg-label">Product Type <span class="req">*</span></label>
                            <select name="ProductType" class="fg-control" required>
                                <option value="Smartphone">Smartphone (IMEI Tracked)</option>
                                <option value="Feature Phone">Feature Phone (IMEI Tracked)</option>
                                <option value="Tablet">Tablet (IMEI / Serial Tracked)</option>
                                <option value="Smartwatch">Smartwatch</option>
                                <option value="Charger">Charger / Adapter</option>
                                <option value="Cable">Cable / Converter</option>
                                <option value="Mobile Cover">Mobile Cover / Case</option>
                                <option value="Tempered Glass">Tempered Glass Screen Guard</option>
                                <option value="Earphones">Earphones / TWS Earbuds</option>
                                <option value="Headphones">Headphones</option>
                                <option value="Power Bank">Power Bank</option>
                                <option value="Memory Card">Memory Card / Flash Drive</option>
                                <option value="Speaker">Bluetooth Speaker</option>
                                <option value="Other">Other Mobile Accessory</option>
                            </select>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <div class="fg">
                            <label class="fg-label" asp-for="Name">Product / Model Title <span class="req">*</span></label>
                            <input asp-for="Name" id="Name" class="fg-control" placeholder="e.g. Samsung Galaxy S23 Ultra" required>
                            <span asp-validation-for="Name" class="fg-error"></span>
                        </div>
                    </div>

                    <div class="col-md-4">
                        <div class="fg">
                            <label class="fg-label">Brand</label>
                            <input type="text" name="Brand" class="fg-control" placeholder="e.g. Samsung, Apple, Vivo">
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="fg">
                            <label class="fg-label">Model Name</label>
                            <input type="text" name="ModelName" class="fg-control" placeholder="e.g. Galaxy S23">
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="fg">
                            <label class="fg-label">RAM / Storage Variant</label>
                            <input type="text" name="Variant" class="fg-control" placeholder="e.g. 12GB RAM / 256GB">
                        </div>
                    </div>

                    <div class="col-md-4">
                        <div class="fg">
                            <label class="fg-label">Color</label>
                            <input type="text" name="Color" class="fg-control" placeholder="e.g. Phantom Black">
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="fg">
                            <label class="fg-label">Warranty (Months)</label>
                            <input type="number" name="WarrantyDurationMonths" value="12" class="fg-control" placeholder="12">
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="fg">
                            <label class="fg-label">Requires IMEI Tracking?</label>
                            <select name="IsImeiRequired" class="fg-control">
                                <option value="true">Yes — IMEI Tracked Device</option>
                                <option value="false">No — Quantity-based Accessory</option>
                            </select>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <div class="fg">
                            <div class="d-flex align-items-center justify-content-between mb-1">
                                <label class="fg-label mb-0" asp-for="Code">SKU / Product Code <span class="req">*</span></label>
                                <button type="button" class="btn-d btn-ghost btn-sm-d py-0" style="height:22px;font-size:11px;padding:0 8px;" onclick="generateSKU()">
                                    <i class="bi bi-magic me-1"></i> Auto Generate
                                </button>
                            </div>
                            <div class="fg-action-wrap">
                                <input asp-for="Code" id="Code" class="fg-control" placeholder="e.g. SAM-S23-256" required>
                                <button type="button" class="fg-action-btn" onclick="generateSKU()" title="Generate SKU Code">
                                    <i class="bi bi-magic"></i> Generate
                                </button>
                            </div>
                            <span asp-validation-for="Code" class="fg-error"></span>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <div class="fg">
                            <div class="d-flex align-items-center justify-content-between mb-1">
                                <label class="fg-label mb-0" asp-for="Barcode">Barcode / UPC <span class="req">*</span></label>
                                <button type="button" class="btn-d btn-ghost btn-sm-d py-0" style="height:22px;font-size:11px;padding:0 8px;" onclick="generateBarcode()">
                                    <i class="bi bi-upc-scan me-1"></i> Auto Generate
                                </button>
                            </div>
                            <div class="fg-action-wrap">
                                <input asp-for="Barcode" id="Barcode" class="fg-control" placeholder="e.g. 890123456789" required>
                                <button type="button" class="fg-action-btn" onclick="generateBarcode()" title="Generate Barcode">
                                    <i class="bi bi-upc-scan"></i> Generate
                                </button>
                            </div>
                            <span asp-validation-for="Barcode" class="fg-error"></span>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <div class="fg">
                            <label class="fg-label" asp-for="CategoryId">Category <span class="req">*</span></label>
                            <select asp-for="CategoryId" asp-items="Model.Categories" class="fg-control" required>
                                <option value="">— Select Category —</option>
                            </select>
                            <span asp-validation-for="CategoryId" class="fg-error"></span>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <div class="fg">
                            <label class="fg-label" asp-for="Status">Status</label>
                            <select asp-for="Status" class="fg-control">
                                <option value="Active">Active</option>
                                <option value="Inactive">Inactive</option>
                            </select>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <div class="fg">
                            <label class="fg-label" asp-for="PurchasePrice">Purchase Price (₹) <span class="req">*</span></label>
                            <div class="fg-prefix-wrap">
                                <span class="fg-prefix">₹</span>
                                <input asp-for="PurchasePrice" class="fg-control" type="number" step="0.01" min="0" placeholder="0.00" required>
                            </div>
                            <span asp-validation-for="PurchasePrice" class="fg-error"></span>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <div class="fg">
                            <label class="fg-label" asp-for="SellingPrice">Selling Price (₹) <span class="req">*</span></label>
                            <div class="fg-prefix-wrap">
                                <span class="fg-prefix">₹</span>
                                <input asp-for="SellingPrice" class="fg-control" type="number" step="0.01" min="0" placeholder="0.00" required>
                            </div>
                            <span asp-validation-for="SellingPrice" class="fg-error"></span>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <div class="fg">
                            <label class="fg-label" asp-for="InitialStock">Initial Stock Level</label>
                            <input asp-for="InitialStock" class="fg-control" type="number" min="0" placeholder="0">
                            <span asp-validation-for="InitialStock" class="fg-error"></span>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <div class="fg">
                            <label class="fg-label" asp-for="MinimumStock">Minimum Safety Stock <span class="req">*</span></label>
                            <input asp-for="MinimumStock" class="fg-control" type="number" min="0" placeholder="e.g. 5" required>
                            <span asp-validation-for="MinimumStock" class="fg-error"></span>
                        </div>
                    </div>

                    <div class="col-12">
                        <div class="fg">
                            <label class="fg-label" asp-for="Description">Description & Key Specs</label>
                            <textarea asp-for="Description" class="fg-control" rows="3"
                                      placeholder="Add processor specs, camera details, battery capacity..."></textarea>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Right Column -->
        <div class="col-lg-4">
            <div class="card-d card-p-lg">
                <div class="s-title">Product Image</div>

                <div class="upload-area" id="upload-area" onclick="document.getElementById('ProductImage').click()">
                    <img id="image-preview" src="/images/default-product.png" alt="Preview"
                         style="max-height:160px;max-width:100%;border-radius:var(--radius-md);object-fit:cover;display:block;margin:0 auto 12px">
                    <div class="upload-area-text">
                        <i class="bi bi-cloud-upload me-1"></i> Click to upload image
                    </div>
                    <div class="upload-area-hint">JPG, PNG or WEBP · Max 5 MB</div>
                </div>

                <input asp-for="ProductImage"
                       id="ProductImage"
                       type="file"
                       accept=".jpg,.jpeg,.png,.webp"
                       style="display:none"
                       onchange="previewImg(this)">
                <span asp-validation-for="ProductImage" class="fg-error mt-2"></span>
            </div>
        </div>
    </div>

    <!-- Actions -->
    <div class="d-flex align-items-center justify-content-end gap-3 mt-4">
        <a href="@Url.Action("Index","Product")" class="btn-d btn-outline">Cancel</a>
        <button type="submit" class="btn-d btn-primary">
            <i class="bi bi-check2"></i> Save Product
        </button>
    </div>
</form>

@section Scripts {
<partial name="_ValidationScriptsPartial" />
<script>
function previewImg(input) {
  if (!input.files[0]) return;
  const reader = new FileReader();
  reader.onload = e => { document.getElementById('image-preview').src = e.target.result; };
  reader.readAsDataURL(input.files[0]);
}

function generateSKU() {
  const nameInput = document.getElementById('Name');
  const codeInput = document.getElementById('Code');
  let prefix = 'MOB';

  if (nameInput && nameInput.value.trim()) {
    const words = nameInput.value.trim().split(/\s+/);
    if (words.length >= 2) {
      prefix = (words[0][0] + words[1][0] + (words[2] ? words[2][0] : words[1][1] || 'X')).toUpperCase();
    } else if (words[0].length >= 3) {
      prefix = words[0].substring(0, 3).toUpperCase();
    }
  }

  const randomNum = Math.floor(100 + Math.random() * 900);
  const sku = `${prefix}-${randomNum}`;
  if (codeInput) {
    codeInput.value = sku;
    if (window.showToast) window.showToast(`Generated SKU: ${sku}`, 'success');
  }
}

function generateBarcode() {
  const barcodeInput = document.getElementById('Barcode');
  const prefix = '890';
  let middle = '';
  for (let i = 0; i < 9; i++) {
    middle += Math.floor(Math.random() * 10);
  }
  const barcode = prefix + middle;
  if (barcodeInput) {
    barcodeInput.value = barcode;
    if (window.showToast) window.showToast(`Generated Barcode: ${barcode}`, 'success');
  }
}
</script>
}
